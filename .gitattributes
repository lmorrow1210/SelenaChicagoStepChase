# Markdown uses two trailing spaces as a hard line break. Exempt .md from the
# trailing-whitespace (blank-at-eol) check so `git diff --check` stays clean
# without collapsing intentional line breaks in the canon docs.
*.md whitespace=-blank-at-eol
