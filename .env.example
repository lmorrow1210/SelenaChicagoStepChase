# ---- apps/api ----
DATABASE_URL=postgres://localhost:5432/selenas_chase
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:4000/api/auth/google/callback
# Verify current Google Health/Fitbit API base before Module 1 (plan §5, flagged decision #3)
HEALTH_API_BASE=https://health.googleapis.com/v4
JWT_SECRET=            # openssl rand -hex 32
TOKEN_ENC_KEY=         # openssl rand -hex 32 (AES-256-GCM key for refresh tokens)
WEB_ORIGIN=http://localhost:3000
CRON_TZ_STRATEGY=per-group
SENTRY_DSN=

# ---- apps/web ----
NEXT_PUBLIC_API_URL=http://localhost:4000
